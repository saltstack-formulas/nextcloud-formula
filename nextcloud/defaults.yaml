# -*- coding: utf-8 -*-
# vim: ft=yaml
---
nextcloud:
  pkg:
    name: nextcloud
  rootgroup: root
  config: '/etc/nextcloud'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
